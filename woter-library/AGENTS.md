# Woter Library - Rules

## Purpose
This file defines rules for shared library changes.

## Source Structure (Required)
- The library source must contain these folders under `src/`:
  - `interfaces`
  - `services`
- Shared API contracts must be created in `src/interfaces`.
- Shared reusable logic must be implemented in `src/services`.
